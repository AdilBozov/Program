function solve(input){
    var jump= parseInt(input[0]);
    var map = parseInt(input[1]);

    function simvol(number){
        for(var i = 0; i<number; i++){
            console.

        }
    }

    simvol(map);


    for (var i=2; i<input.length; i++) {
        numberGame = input[i].split(", ");
        console.log(numberGame);



    }
}

solve(['10','19','angel, 9','Boris, 10','Georgi, 3','Dimitar, 7']);